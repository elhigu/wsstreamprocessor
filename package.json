{
  "name": "visuline",
  "version": "0.0.0",
  "description": "Express server to send frames / packets of data over websocket for browser to process",
  "main": "server.js",
  "dependencies": {
    "express": "^4.10.4",
    "socket.io": "^1.2.1"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Mikael Lepistö",
  "license": "MIT"
}
